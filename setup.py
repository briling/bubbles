from setuptools import setup

setup(
    name='bubbles',
    version='0.1',
    description='the ultimate bubble generator',
    url='https://github.com:briling/bubbles.git',
    install_requires=[],
    packages=['bubbles'],
)
